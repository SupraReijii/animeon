#!/bin/bash -e

# If running the rails server then create or migrate existing database
./bin/rails db:prepare
#mount -t nfs4 -v 79.120.9.56:/videos /rails/public/files/video
#mount -t nfs4 -v 79.120.9.56:/posters /rails/public/files/posters
exec "${@}"
