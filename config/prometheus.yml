global:
  scrape_interval: 3s

scrape_configs:
  - job_name: "animeon"
    tls_config:
      cert_file: /prometheus/fullchain.pem
      key_file: /prometheus/privkey.pem
    metrics_path: /metrics
    scheme: https
    static_configs:
      - targets: ["animeon.ru:443"]
